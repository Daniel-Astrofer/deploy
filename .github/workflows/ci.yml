name: Deploy CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: bash scripts/check_architecture_guardrails.sh

  security:
    uses: Daniel-Astrofer/.github/.github/workflows/security-scan.yml@3cfcdc1789bbd58a7e7e307bb08cd1c59159e161
