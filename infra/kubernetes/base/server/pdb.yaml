apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: server
  labels:
    app.kubernetes.io/name: server
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: server
