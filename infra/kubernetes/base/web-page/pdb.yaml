apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web-page
  labels:
    app.kubernetes.io/name: web-page
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: web-page
