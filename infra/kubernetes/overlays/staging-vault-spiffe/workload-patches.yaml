apiVersion: apps/v1
kind: Deployment
metadata:
  name: vault
spec:
  template:
    metadata:
      labels:
        kerosene.io/spiffe-role: vault
    spec:
      containers:
        - name: vault
          env:
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spire/sockets/spire-agent.sock
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /run/spire/sockets
              readOnly: true
      volumes:
        - name: spiffe-workload-api
          csi:
            driver: csi.spiffe.io
            readOnly: true
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vault-tor
spec:
  template:
    metadata:
      labels:
        kerosene.io/spiffe-role: node
        kerosene.io/discovery-plane: vault
    spec:
      serviceAccountName: kerosene-node
      containers:
        - name: kerosene-node
          env:
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spire/sockets/spire-agent.sock
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /run/spire/sockets
              readOnly: true
      volumes:
        - name: spiffe-workload-api
          csi:
            driver: csi.spiffe.io
            readOnly: true
